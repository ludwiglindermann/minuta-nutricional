package com.duoc.minutanutricional.model

// interfaz: define un contrato (una propiedad que toda clase que la implemente
// debe entregar), sin decir como se guarda el dato por dentro
interface Recomendable {
    val recomendacionNutricional: String
}
